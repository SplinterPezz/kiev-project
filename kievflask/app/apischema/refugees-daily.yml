Example endpoint returning the list of Refugees aggregated
---
parameters:
  - name: days
    in: path
    type: int
    required: true
    default: 1
responses:
  200:
    schema:
      $ref: '#/definitions/Chart'