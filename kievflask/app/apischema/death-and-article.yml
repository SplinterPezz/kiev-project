Example endpoint returning the list of Articles and Death values
---
parameters:
  - name: days
    in: path
    type: int
    required: true
    default: 1
responses:
  200:
    schema:
      $ref: '#/definitions/Chart'